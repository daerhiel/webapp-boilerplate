import { GraphPicturePipe } from './graph-picture.pipe';

describe('GraphPicturePipe', () => {
  it('create an instance', () => {
    const pipe = new GraphPicturePipe();
    expect(pipe).toBeTruthy();
  });
});
