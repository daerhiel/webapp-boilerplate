import { DefaultPictureDirective } from './default-picture.directive';

describe('DefaultPictureDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultPictureDirective();
    expect(directive).toBeTruthy();
  });
});
